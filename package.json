{
  "name": "flowfuel",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "date-fns": "^2.30.0",
    "framer-motion": "^10.18.0",
    "idb": "^8.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "proxy": "http://localhost:5050",
  "scripts": {
    "start": "react-scripts start",
    "dev:api": "node scripts/dev-api-server.mjs",
    "dev": "concurrently -n web,api -c blue,green \"npm:start\" \"npm:dev:api\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "audit-ci": "^7.1.0",
    "concurrently": "^10.0.5",
    "dotenv": "^17.4.2"
  }
}
